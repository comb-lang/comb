// The type of the main function must be `() -> Int` when compiling via C
// The type of the main function can be `() -> Int` or `(Compiler) -> Int` when interpreting
main = |a: String, b: Int| -> Int {
    println(a & b.to_str)
	return 0
}
